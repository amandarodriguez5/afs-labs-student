apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  namespace: arodriguezsemidey
  labels:
    app: app
spec:
  replicas: 1
  strategy:
    type: Recreate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  minReadySeconds: 20
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: feature-toggle
        - secretRef:
            name: db-secret
        name: app-deployment
        image: harbor.dev.afsmtddso.com/arodriguezsemidey-harbor-project/app:edd8bc2-b7bb019
        ports:
        - containerPort: 5000
